# Local secrets / certificates
.env
.env.*
!.env.example
*.pfx
*.p12
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/
.cache/historical_lab/

# Private prediction-engine state.  The public app receives only compact JSON
# summaries; immutable model observations and outcomes stay in SQLite.
.prediction_engine/
*.sqlite3-wal
*.sqlite3-shm

# Local Fubon helper files
fubon_local_config.json

# Tiingo's free plan is internal-use only. Raw caches are persisted through
# GitHub Actions cache and must never be committed or served by GitHub Pages.
reports/tiingo_us_close_fallback.json
reports/tiingo_us_close_staging.json
reports/owner_private_holding_simulation.json
