# 安全性政策

## 支援範圍

目前只支援 `main` 分支的最新版本。測試、影子驗證與正式下單必須維持既有隔離；安全修正不得順便變更股票排名、權重或歷史結果。

## 回報安全問題

請使用 GitHub 儲存庫的 **Security → Report a vulnerability** 私密回報功能。不要在公開 Issue、討論區或截圖中貼出 API Key、Token、券商帳密、憑證、持股資料或可重現真實下單的資訊。

回報時請提供：

- 受影響功能與版本或提交編號。
- 可安全重現的步驟；請使用假資料與測試帳號。
- 可能影響，以及是否涉及本人版、朋友版或後端服務。
- 建議修正方式（若有）。

## 處理原則

- 收到私密回報後，目標為 3 個工作天內完成初步分級。
- 若可能洩漏密鑰，先撤銷與輪替密鑰，再進行程式修正。
- 若可能觸發真實下單，維持 `DEPLOYMENT_ENVIRONMENT=test`、`TRADING_MODE=paper` 與 `LIVE_TRADING_ENABLED=false`，直到修正及驗收完成。
- 修正完成前，不公開可被利用的細節。

本專案目前沒有公開漏洞獎金計畫。
